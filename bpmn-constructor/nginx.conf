server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: все маршруты (включая /editor, /my-schemas и т.д.)
    # обрабатываются клиентским роутером.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
